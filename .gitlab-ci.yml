stages:
  - lint
  - test

default:
  image: python:3.12-slim
  before_script:
    - python -m pip install --upgrade pip
    - pip install -r requirements.txt

lint-code:
  stage: lint
  script:
    - ruff check .

run-tests:
  stage: test
  script:
    - pytest
